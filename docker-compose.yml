version: '3.8'

services:
  lifetracker:
    build: .
    container_name: lifetracker
    ports:
      - "8129:8000"
    environment:
      - DATABASE_FILE=/app/data/tracker.db
    volumes:
      - lifetracker_db:/app/data
    restart: unless-stopped

volumes:
  lifetracker_db:
